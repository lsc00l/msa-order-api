apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    orgId: 1
    url: http://prometheus:9092
    basicAuth: false
    isDefault: true
    editable: true
    version: 1
    jsonData:
      timeInterval: "5s"
      httpMethod: "POST"